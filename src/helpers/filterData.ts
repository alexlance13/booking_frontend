export default function filter(params: any) {
  const rules = {
    pf: undefined, // filter by price from
    pt: undefined, // filter by price to
    rn: undefined, // filter by rooms number
    startDate: undefined, // filter by start date
    endDate: undefined, // filter by end date
  };
}
